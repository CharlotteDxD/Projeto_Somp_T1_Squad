# ==========================================================================
# Sompo Predict — dependências
# ==========================================================================
# Instalação:  pip install -r requirements.txt

# --- Painel (app.py) ---
streamlit>=1.32.0
plotly>=5.20.0

# --- Serviço de ingestão (api_telemetria.py) ---
# Sem estes, o painel roda mas a telemetria não é recebida.
flask>=3.0.0
gunicorn>=21.2.0          # produção (wsgi.py); não é usado no Windows

# --- Cálculo e dados ---
numpy>=1.26.0
pandas>=2.2.0
openpyxl>=3.1.0           # leitura de data/susep_real.xlsx, quando presente

# --- Segurança ---
PyJWT>=2.8.0
cryptography>=42.0.0      # Fernet (AES) na trilha de auditoria
argon2-cffi>=23.1.0       # hash de senha

# --- Comunicação com o serviço de ingestão ---
requests>=2.31.0

# --- Modelagem e avaliação (scripts/) ---
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0             # carga de data/modelo_xgboost.pkl
